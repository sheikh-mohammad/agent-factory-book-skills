{
  "name": "chatkit-app",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "backend": "uvicorn app.main:app --reload --port 8000",
    "frontend": "vite --port 5170",
    "start": "concurrently \"npm run backend\" \"npm run frontend\"",
    "test": "vitest",
    "lint": "eslint src"
  },
  "dependencies": {
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "concurrently": "^8.2.2",
    "vitest": "^1.0.0",
    "eslint": "^8.55.0"
  }
}
